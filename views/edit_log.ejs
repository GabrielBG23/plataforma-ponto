<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Ponto</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 h-screen flex items-center justify-center p-4">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        
        <h2 class="text-xl font-bold mb-6 text-gray-800 border-b pb-2">✏️ Editar Registro</h2>
        
        <p class="mb-4 text-sm text-gray-600">
            Funcionário: <strong class="text-gray-900 text-lg ml-1"><%= log.name %></strong>
        </p>

        <form action="/admin/edit/<%= log.id %>" method="POST">
            <div class="mb-4">
                <label class="block text-gray-700 text-sm font-bold mb-2">Data e Hora</label>
                <input type="datetime-local" name="new_timestamp" 
                    value="<%= moment(log.timestamp).format('YYYY-MM-DDTHH:mm') %>"
                    class="w-full p-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500" required>
            </div>

            <div class="mb-6">
                <label class="block text-gray-700 text-sm font-bold mb-2">Tipo de Registro</label>
                <select name="new_type" class="w-full p-2 border border-gray-300 rounded bg-white focus:outline-none focus:ring-2 focus:ring-blue-500">
                    <option value="ENTRADA" <%= log.type === 'ENTRADA' ? 'selected' : '' %>>🟢 ENTRADA</option>
                    <option value="INTERVALO_IDA" <%= log.type === 'INTERVALO_IDA' ? 'selected' : '' %>>☕ INTERVALO (IDA)</option>
                    <option value="INTERVALO_VOLTA" <%= log.type === 'INTERVALO_VOLTA' ? 'selected' : '' %>>🔙 INTERVALO (VOLTA)</option>
                    <option value="SAIDA" <%= log.type === 'SAIDA' ? 'selected' : '' %>>🔴 SAÍDA</option>
                </select>
            </div>

            <div class="flex justify-between items-center mt-8">
                <a href="/admin" class="text-gray-500 hover:text-gray-700 font-medium">Cancelar</a>
                <button type="submit" class="bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700 shadow transition">
                    Salvar Alterações
                </button>
            </div>
        </form>
    </div>
</body>
</html>